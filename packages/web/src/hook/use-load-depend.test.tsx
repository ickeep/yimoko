import { useLoadDepend } from './use-load-depend';

describe('use-load-depend', () => {
  test('useLoadDepend', () => {
    // const [loading, errs, load] = useLoadDepend([undefined, undefined]);
    // expect(loading).toBe(false);
    // expect(errs).toEqual([]);
    // expect(load).toBeInstanceOf(Function);
  });
});
